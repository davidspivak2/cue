# Local scripts

Put personal or machine-only helper scripts here. This directory is listed in `.gitignore` (except this file), so nothing you add here will be committed.

Shared, documented workflows stay in the parent `scripts/` folder.
